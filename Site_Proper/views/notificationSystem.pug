doctype html
html(lang="en")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		title Notifications
		link(href="https://fonts.googleapis.com/css?family=Work+Sans:400", rel="stylesheet")
		link(rel="stylesheet", href="styles.css")

	body
		header
			.navbar
				span Notification System
				nav
					ul
						li: a(href="homepage.html") Home
						li: a(href="aboutus.html") About Us

		.loginPage
			.login-box(style="overflow-y: auto; width: 500px; min-height: 500px; padding: 20px;")
				h2 Notifications	
				if notifications.length
					div(style="overflow-y: auto; padding: 10px; margin: 10px auto; width: 90%;")
						each note in notifications
							div(style="background-color: #D3D9D4; color: #124E66; border-radius: 10px; padding: 10px; margin: 15px auto; width: 90%;")
								strong From: Volunteer Central
								br
								| #{note.message}
				else
					p(style="text-align: center; color: light-gray; margin-top: 20px;") No notifications yet.
				form(method="POST", action="/send-notification", style="margin-top: 20px; width: 90%;")
					div(style="margin-bottom: 10px;")
						label(for="email") Send To Email:
						input(type="text", name="email", required, style="width: 100%; padding: 8px; border-radius: 10px; border: none;")

					div(style="margin-bottom: 10px;")
						label(for="message") Message:
						textarea(name="message", required, style="width: 100%; height: 100px; padding: 8px; border-radius: 10px; border: none;")

					button(type="submit", style="width: 100%; height: 40px; border-radius: 20px; background-color: #D3D9D4; color: #124E66; border: none; margin-top: 10px;")
						| Send Notification
